{% if cart.item_count > 0 %}
{% else %}
  <div class="max-w-6xl h-60 my-4 flex items-center justify-center">
    <div class="text-center">
      <h1 class="text-3xl my-4">Your Cart</h1>
      <div class="my-4 py-4">
        <a
          href="{{ routes.all_products_collectiom_url }}"
          class="boreder boreder-pink-300 text-black bg-pink-600 px-8 py-3"
          >Continue shopping</a
        >
      </div>
    </div>
  </div>
{% endif %}
